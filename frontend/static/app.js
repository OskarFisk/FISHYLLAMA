const chat=document.querySelector('#chat'),form=document.querySelector('#form'),input=document.querySelector('#input'),provider=document.querySelector('#provider'),messages=[];function add(role,text){const e=document.createElement('div');e.className='msg '+role;e.innerHTML='<b>'+role+'</b><p></p>';e.querySelector('p').textContent=text;chat.appendChild(e);chat.scrollTop=chat.scrollHeight;return e}form.addEventListener('submit',async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;input.value='';messages.push({role:'user',content:text});add('user',text);const load=add('assistant','Thinking...');try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages,provider:provider.value,model:'auto'})});const d=await r.json();if(!r.ok)throw Error(d.detail||'Request failed');load.querySelector('p').textContent=d.content;messages.push({role:'assistant',content:d.content})}catch(err){load.querySelector('p').textContent='Error: '+err.message}});